import {
    Box,
    // , useMediaQuery
} from "@mui/material";
import Form from "./form";

const Login = () => {
    // const isNonMobileScreens = useMediaQuery("(min-width: 1000px)");

    return (
        <Box>
            <Form />
        </Box>
    );
};

export default Login;
